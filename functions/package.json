{
  "name": "geocharge-functions",
  "description": "Cloud Functions for GeoCharge — branded email verification via Zoho SMTP",
  "engines": {
    "node": "24"
  },
  "main": "index.js",
  "scripts": {
    "test": "node --test",
    "serve": "firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions",
    "logs": "firebase functions:log"
  },
  "dependencies": {
    "firebase-admin": "^14.3.0",
    "firebase-functions": "^7.3.2",
    "google-auth-library": "^10.9.1",
    "nodemailer": "^6.9.16"
  },
  "private": true
}

// Basic smoke test for the GeoCharge admin app.
//
// The app requires Firebase initialisation, so a full widget pump isn't done
// here. This just verifies the test harness runs.
import 'package:flutter_test/flutter_test.dart';

void main() {
  test('placeholder', () {
    expect(1 + 1, 2);
  });
}
